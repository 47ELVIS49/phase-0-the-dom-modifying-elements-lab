// Creating a new header element
const newHeader = document.createElement('h1');
newHeader.id = 'victory';

// Setting the inner text of the new header
newHeader.textContent = 'Elvis Momanyi is the champion';

// Appending the new header to the body
document.body.appendChild(newHeader);
